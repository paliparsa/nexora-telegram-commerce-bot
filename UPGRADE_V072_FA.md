# ارتقا به Nexora v0.7.2

این نسخه باگ دکمه «🔎 بررسی تنظیم BEP20» را رفع می‌کند. در v0.7.1 callback ابتدا بدون متن پاسخ داده می‌شد و سپس کد دوباره تلاش می‌کرد نتیجه را با همان callback نمایش دهد؛ تلگرام پاسخ دوم را نمایش نمی‌داد.

در v0.7.2 نتیجه تست داخل همان پیام پنل نمایش داده می‌شود و علاوه بر تنظیمات، اتصال واقعی به Etherscan API نیز بررسی می‌شود.

## ارتقا
1. فایل‌های ZIP را روی Repo فعلی جایگزین کن.
2. `wrangler.jsonc` فعلی و Database ID خودت را نگه دار.
3. Commit/Push کن و منتظر Deploy Cloudflare بمان.
4. در بات: مدیریت → تنظیمات پرداخت → «🔎 بررسی تنظیم BEP20».
5. باید صفحه نتیجه شامل Wallet، Contract، Chain ID، API Key و API connection نمایش داده شود.

Migration جدیدی ندارد.

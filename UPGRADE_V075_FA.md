# ارتقا به v0.7.5

این نسخه migration جدید ندارد. فایل‌ها را روی Repo فعلی جایگزین و Commit کنید؛ `wrangler.jsonc` فعلی خودتان را نگه دارید.

## تغییرات

- Rate Engine: CoinGecko → Kraken → Binance Spot → Binance Avg → Cache → Manual.
- منوی پرداخت: تست‌ها داخل «مرکز تست پرداخت» مرتب شده‌اند.
- BEP20: تا زمانی که یک فاکتور تست واقعی با تراکنش صحیح و confirmations کافی شناسایی نشود، LIVE به‌صورت ایمنی قفل است.
- پس از تست موفق، `bep20_live_verified=1` در تنظیمات داخلی ثبت می‌شود و ادمین می‌تواند LIVE را فعال کند.

## تست BEP20

1. مدیریت → مالی و پرداخت → تنظیمات پرداخت → مرکز تست پرداخت.
2. «فاکتور تست واقعی BEP20» را بزنید.
3. مبلغ دقیق نمایش‌داده‌شده را به آدرس نمایش‌داده‌شده روی BSC ارسال کنید.
4. بعد از confirmations کافی، بات پیام موفقیت تست می‌دهد ولی Credit اضافه نمی‌کند.
5. بعد از آن می‌توانید BEP20 را LIVE کنید.

## Rate Engine

اگر CoinGecko روی Cloudflare 429 بدهد، Kraken به‌عنوان fallback بعدی استفاده می‌شود. اگر آن هم در دسترس نبود Binance و در نهایت cache/manual استفاده می‌شوند.

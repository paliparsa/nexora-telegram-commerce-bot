# ارتقا به Nexora v0.7.4

این نسخه migration جدید ندارد و روی v0.7.3 به‌صورت مستقیم قابل جایگزینی است.

## تغییرات اصلی

- منوی کاربر مرتب‌تر و کم‌ازدحام‌تر شده است.
- پنل مدیریت به ۴ بخش تقسیم شده: فروش و محتوا، مالی و پرداخت، کاربران و پشتیبانی، سیستم و امنیت.
- صفحه تنظیمات پرداخت مرتب‌تر شده و تست‌های هر روش در یک بخش منظم قرار گرفته‌اند.
- برای BEP20 در حالت TEST دکمه «ساخت فاکتور تست واقعی BEP20» اضافه شده است. این فاکتور با مبلغ پایه 0.10 دلار ساخته می‌شود و چون TEST Mode فعال است، با شناسایی تراکنش Credit اضافه نمی‌شود؛ فقط موفق بودن اسکن اعلام می‌شود.
- Rate Engine مقاوم‌تر شده است: CoinGecko → Binance spot → Binance average → نرخ Cache شده → نرخ دستی.
- اگر CoinGecko با HTTP 429 محدود شود ولی fallback یا cache موجود باشد، Rate Engine دیگر به اشتباه از کارافتاده نمایش داده نمی‌شود.
- منبع آخرین نرخ TON در تنظیمات ذخیره و در پنل نمایش داده می‌شود.

## روش ارتقا

1. محتوای ZIP را روی Root همان Repo فعلی GitHub Replace کن.
2. `wrangler.jsonc` فعلی خودت را نگه دار؛ این بسته عمداً آن را تغییر نمی‌دهد.
3. Commit به `main` بزن.
4. Cloudflare به‌صورت خودکار Deploy می‌کند.
5. چون migration جدیدی وجود ندارد، دیتابیس فعلی دست‌نخورده می‌ماند.
6. بعد از Deploy وارد بات شو و از مسیر `مدیریت → مالی و پرداخت → تنظیمات پرداخت` تست‌ها را اجرا کن.

## تست واقعی BEP20

تا زمانی که تست واقعی انجام ندادی BEP20 را روی `TEST` نگه دار.

از صفحه تنظیمات پرداخت روی «ساخت فاکتور تست واقعی BEP20» بزن. بات فاکتور کوچک آزمایشی می‌سازد. مبلغ دقیق همان فاکتور را به Wallet تعیین‌شده بفرست. Scanner باید تراکنش را پیدا کند و پیام «تست BEP20 موفق بود» برای ادمین بفرستد، اما Credit کاربر را افزایش ندهد.

بعد از موفقیت تست، می‌توانی BEP20 را دستی روی LIVE بگذاری.
